# PAUL Handoff — 2026-04-22

**Date:** 2026-04-22 (afternoon pause, user resuming evening)
**Status:** Paused — heavy emotional/context day; user needed a break.

---

## READ THIS FIRST

Fresh Claude: the user is returning from a hard session. Two things happened today that hit him:

1. **Ben Cohen at Final sent an email** flagging that LookInsight was naming buyside firms (Citadel, Squarepoint, Final) in outreach to other firms. Called it "extremely unprofessional." User acknowledged in writing, scrubbed names from deck + future outreach policy. Incident resolved, but it stung.

2. **Data audit surfaced a methodology-cohort mismatch.** 47 of the 141 v1.1 brief signals (33%) have underlying transactions ALL currently classified FILTERED — because the classifier rejects them for "Earnings beyond 60d threshold." The published brief names earnings-proximity (≤60d) as a core filter, but `compute_all` doesn't enforce it at SignalPerformance creation. The brief cohort includes signals that violate its own stated methodology.

At the point of pause, user said *"feel like quitting."* I reassured him: the signals and returns are real, the problem is narrative not data, the fix is an afternoon's work. He agreed to step away. **Be gentle when he's back. Don't pile on.**

User's call coming out of the session: **remove earnings-proximity from future pitch language.** Reposition product as "multi-insider conviction clusters on midcap US equities" — which is accurate and still wins.

---

## Current State

**Version:** 1.6.0 (complete) — no active milestone. v1.7 scoping deferred.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Idle — mid data-audit, methodology revision pending]
```

**DB state (post-cleanup):**
- Mature strong_buy: 142 (unchanged by cleanup)
- Immature: 57
- Total InsiderTransactions: ~57,931 (down from 58,237 after deleting 374 pre-2020)
- Persons: 13,931 (down from 26,148 after deleting 12,217 orphans — most were pre-existing dark matter)
- ActivistFilings: 7,579 (latest 2026-04-20)

---

## What Was Done Today

### Morning — memory + administrative cleanup
- Applied STALE banners to 6 historical memory files (signal_truth_what_works, conviction_tests_apr12_corrected, research_test_results_apr2026, signal_autoresearch_findings, research_vs_data_crossref, competitive-landscape).
- Stripped specific numerical claims from `MEMORY.md` (Data Coverage counts, Proof Wall tickers, Scanner Status totals, Alpha Improvements hit rates, Compound Signal Engine details).
- Created `feedback_no_naming_funds.md` — hard rule: never name buyside firms without explicit written consent.
- Created `feedback_no_inline_python.md` — write scripts to `.py` files; no more `-c "..."` with `#` comments.

### Outreach fallout from Ben/Final
- Drafted acknowledgment reply to Ben (user sent and Ben acknowledged).
- Scrubbed deck: removed academic-foundation slide, competitive-map slide, and fund names from Slide 12. Deck is now 10 slides (was 12).
- Drafted polite request to Jinang (Neudata) to remove any references to Citadel/Final/Squarepoint from Neudata-published materials. User sent it.

### Data cleanup
- `sanity_check_01_malformed_dates.py` — 1 typo record ("0024-05-23"), plus ~379 legitimate pre-2020 records from old CIKs.
- `sanity_check_01c_cleanup_pre2020.py` — DELETED 374 pre-2020 InsiderTransactions (370 NULL-classification + 4 FILTERED). PRESERVED 5 GENUINE pre-2020.
- Side effect: cleanup also removed 12,217 orphan Persons (most were pre-existing dark matter, not caused by this delete).
- `sanity_check_01e/01f` — flagged the 5 GENUINE pre-2020 as `late_disclosure=true` with `late_disclosure_days` computed (avg ~10 years, max 20 years lag — these are late-filed disclosures, not errors).

### Sanity checks
- **Check 1 — malformed dates:** ✅ cleaned.
- **Check 2 — orphan transactions:** ✅ all 57,931 InsiderTransactions are fully linked (Company + Person).
- **Check 3 — cluster integrity:** 192/199 (96.5%) clean. 7 mismatches all in direction `actual > stored` by 1–2 — caused by the cluster service's `(date, shares, price)` dedup at `insider_cluster_service.py:307-322` collapsing different Persons into one trade when values coincide. **Not data corruption, just a subtle under-count in cluster service.**

### Signal audit — THE HEADLINE FINDING
- `signal_audit_2026-04-22.py` generated flag-ranked audit cards for the 142-mature cohort.
- Flag prevalence across 142:
  - `all_filtered`: **47 (33%)**
  - `single_day_cluster`: 21
  - `insider_count_drift`: 6
  - `no_genuine_txns`: 4
  - `IPO_proximity`: 2 (1 false positive — GAM)
  - `single_price_cluster`: 1
- `filter_reason_analysis_2026-04-22.py` — all 47 `all_filtered` cases have the SAME root cause: classifier rejects them for "Earnings in Xd — beyond 60d threshold."
- `crosscheck_brief_vs_filtered_2026-04-22.py` — 47 of the 141 v1.1 brief signals are in the all_filtered set.

### Numbers — how the published cohort actually partitions
| | All 141 (published) | Passes earnings (94) | **Fails earnings (47)** |
|---|---|---|---|
| Hit rate | 67.4% | 64.9% | **72.3%** |
| Avg return | +14.23% | +14.54% | +13.62% |
| Avg alpha | +8.96pp | +9.97pp | +6.93pp |

The earnings filter isn't strengthening the cohort on hit rate; it raises alpha magnitude but doesn't dominate. **Removing it from methodology language is defensible and honest.**

---

## What's In Progress / Pending

### Decided, not yet executed
- **Remove earnings-proximity from all future outreach language.** Deck and methodology narrative need updating. Reframing: "multi-insider conviction clusters on midcap US equities."
- **Brief PDF regeneration** — may or may not happen. The v1.1 brief is already shipped to clients. Future iterations (v1.2 brief, "methodology update" doc) should reflect the new framing.

### Undone (lower priority, can wait)
- Sanity checks 4–10 (maturity-flag correctness, return arithmetic, classification-override audit, Person duplication, price_series coverage, SPY freshness, eyeball GENUINE sample). User wanted chunk-by-chunk, we stopped at Check 3 + audit.
- v1.7 scoping via `/paul:discuss-milestone`.
- The 67K figure in the deck/brief funnel — still not re-derived from pipeline state.
- Commit today's untracked work to git. Nothing has been committed all session. At-risk files if workspace clears: `signal_audit_2026-04-22.py`, `filter_reason_analysis_2026-04-22.py`, `crosscheck_brief_vs_filtered_2026-04-22.py`, `sanity_check_*.py`, `build_deck.py` (already modified), `LookInsight_Deck.pptx`, memory notes, `.paul/HANDOFF-2026-04-22.md` (this file).
- Apr 21 Form 4 daily pipeline run + compute_all. Today's catch-up is still needed for Apr 21 ingest.

### Watching for
- **Jinang's response** to the deck-update / remove-names email.
- **Ben** — acknowledged and closed, no further action needed.
- **Bridgewater (Jason)** — no reply yet to the pitch sent 2026-04-21.

---

## Key Artifacts Created Today

| File | Purpose |
|---|---|
| `backend/sanity_check_01*.py` | date + orphan cleanup scripts |
| `backend/sanity_check_02_orphan_txns.py` | orphan transaction check |
| `backend/sanity_check_03*.py` | cluster integrity, drift breakdown, deep-dive |
| `backend/signal_audit_2026-04-22.py` | per-signal audit card generator |
| `backend/signal_audit_2026-04-22.md` | first 10 cards (sorted by flag count) |
| `backend/filter_reason_analysis_2026-04-22.py` | earnings-proximity diagnostic |
| `backend/crosscheck_brief_vs_filtered_2026-04-22.py` | brief vs filter intersection |
| `backend/query_coverage_counts.py` | Jinang coverage reply source |
| `LookInsight_Deck.pptx` | 10-slide deck (firm names removed, academic + competitor slides removed) |
| `build_deck.py` | deck generator (modified) |
| `.paul/HANDOFF-2026-04-22.md` | this file |

---

## Memory Notes Added/Updated Today

- `feedback_no_naming_funds.md` (new) — never name buyside firms without consent
- `feedback_no_inline_python.md` (new) — write `.py` files, not `-c "..."`
- `daily_pipeline_gaps.md` (expanded) — now documents 4 gaps (+ scanner-checkpoint-advance-on-upstream-failure from yesterday)
- `analysis_13d_overlap_apr21.md` (yesterday) — 13D × cluster overlap finding
- 6 STALE banners on historical analysis files
- `MEMORY.md` index — multiple stale number strips, reorganized sections

---

## The Critical Decision Left Open

**The v1.1 brief cohort includes 47 signals that fail our published methodology.** Three paths:

1. **Tighten retroactively** — republish a brief with only the 94 passing signals. New headlines: 65% HR, +10pp alpha, 94 mature signals. Smaller cohort, honest methodology.
2. **Reframe methodology** — keep the 141 cohort; change the brief to say earnings-proximity is "informational only" rather than a hard filter. Drop the p=0.003 claim.
3. **Drop the rule entirely** — both in messaging and in filter code. Product becomes "multi-insider midcap conviction clusters" full-stop. Simplest narrative; most internally consistent.

User's current lean: **option 3**. This is what he expressed before pausing ("earnings proximity we can remove from our future mails").

**Don't act on this without a follow-up conversation when he's back.** He was emotionally tired when he made the call; fresh him may want to revisit.

---

## Resume Instructions

1. When user returns, greet gently. Don't open with the audit findings.
2. Ask what he wants to work on first (don't assume).
3. Possible priorities in likely order:
   - (a) Review the methodology decision from a fresh perspective — option 1/2/3 above.
   - (b) Commit today's work to git so nothing is lost.
   - (c) Run Apr 21 Form 4 daily pipeline (it's a day behind).
   - (d) Sanity checks 4–10 (if he wants to keep auditing).
4. Do NOT run destructive operations without explicit confirmation. Do NOT re-ship any artifact externally until the methodology decision is settled.

Full chronological detail of today's findings is above. The scripts in `backend/` are all read-only or explicitly destructive-with-dry-run-preview; re-runnable.

---

*Handoff created: 2026-04-22 afternoon*
