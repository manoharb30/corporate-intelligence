# HANDOFF — 2026-04-17 (session 2)

**Status:** Pausing before critical rewrite of signal_performance_service.py
**Reason:** Most important calculation in the product — needs full plan + TDD, not a rush job

## What happened this session

### Plan 03-02: Backend Cleanup — COMPLETE
- Removed 17 routes, kept 7 (health, snapshot, event-detail, signal-performance, explorer, scanner, activist)
- Deleted 11 orphaned service files
- main.py: 7 include_router lines (was 24)

### Dashboard Design — Brainstorming COMPLETE
- Visual direction: Minimal/Modern (C) — Linear/Stripe style
- Signal List: Card layout (B) with role breakdown
- Signal Detail v2: buyers table + SEC links + hostile flag + buy history + activist + cross-company connections
- Performance Tracker: filterable table (30d/90d/year/month), dynamic header stats, CSV
- Explorer: embedded in Signal Detail (conditional)
- Hero/pricing/contact: parked for later
- Signal List = dashboard + home page: stats bar at top (signals tracked, HR, alpha, active), signal cards below
- Design spec saved: docs/superpowers/specs/2026-04-17-dashboard-redesign-design.md

### Signal Pattern Research (03-03) — COMPLETE
- Buy/mcap sweet spot discovered: 0.01-0.2% = ~78% HR vs ~63% flagged
- p=0.112 (directional, not yet significant)
- Projected p=0.048 at ~195 signals
- Decision: confidence tiers (flag, don't filter) until statistically proven

### Backfill Jun-Nov 2024 (03-04) — COMPLETE
- All 6 months backfilled through full pipeline
- Nov+Oct: 62 min, Sep-Jun: 155 min
- Pipeline bottlenecks fixed: persistent yfinance cache, 429 retry, O(1) lookup, etc.
- 2 new prefilter rules added (BDC issuer 0% FP, underwritten offering 2% FP)
- LLM workers increased to 15
- Batch classification tested — 70% match rate (too low), single-call 98% — kept single-call

### Data Verification (03-05) — IN PROGRESS (paused here)
- Step 1: Backfill complete ✅
- Step 2: Verified data, found Oct-Nov 2025 missing earnings filter — FIXED (re-patched + re-ingested)
- Step 3: Coverage backfill done ✅ — resolved 81 tickers from SEC CIK mapping
- Step 4: Coverage 94.5% for strong_buy candidates — PASSED ✅
- Step 5: SignalPerformance recompute — **NOT DONE** — critical issues found in code review

### Signal Performance Service — CRITICAL ISSUES FOUND
Expert code review identified 6 issues, 2 critical:

1. **CRITICAL: Stale nodes never cleaned up** — MERGE updates existing but never deletes dissolved clusters. Old SignalPerformance nodes from clusters that no longer exist (due to reclassification) remain as stale data. Likely cause of 131→252 unexplained jump.

2. **CRITICAL: Uses live yfinance, not stored price_series** — Calls yfinance for every signal (~900 HTTP calls, 15-30 min). Company.price_series already has the data. Live calls make results inconsistent (different each day).

3. **IMPORTANT: conviction_tier uses current market cap** — Historical signals classified by today's market cap, not signal-date market cap. A $500M company in 2024 that grew to $15B in 2026 gets misclassified.

4. **MODERATE: SPY window may be too short** for oldest signals
5. **MODERATE: Thread pool of 6 may trigger yfinance rate limits**
6. **LOW: Individual Neo4j MERGE instead of batch UNWIND**

### User Decision
User wants a complete rewrite of signal_performance_service.py with:
- Full plan before implementation
- TDD (superpowers skill) — tests first
- Use stored price_series and market_cap, not live yfinance
- Delete stale nodes before recomputing
- Fix conviction_tier to use signal-date market cap
- This is the most critical calculation — no rushing

## Current data state
- Neo4j: GENUINE P transactions from Jun 2024 → Apr 2026 (all months have earnings filter)
- Coverage: 94.5% of strong_buy candidates have market_cap + price_series
- SignalPerformance: STALE — 252 mature strong_buy from premature recompute, includes stale nodes
- DO NOT trust current SignalPerformance numbers until rewrite + recompute

## Files modified this session
- backend/app/main.py — 7 routes (was 24)
- backend/app/services/signal_filter.py — persistent cache
- backend/filter_form4_p_only.py — 429 retry, docstring fix
- backend/ingest_genuine_p_to_neo4j.py — O(1) lookup, removed redundant exists-check
- backend/classify_p_with_prefilter.py — 2 new rules (Rule 20 BDC, Rule 21 underwritten)
- backend/batch_llm_classify.py — 15 workers (was 10)
- backend/run_week.py — SEC_HOLIDAYS rename
- backend/run_month.py — SEC_HOLIDAYS import
- frontend/ — cleanup (03-01), Signal List page with period toggles
- 17 route files deleted, 11 service files deleted
- Oct-Nov 2025 classified JSONs patched with earnings filter

## Resume instructions
```
/paul:resume
```
Next action: Plan the rewrite of signal_performance_service.py using PAUL + TDD superpowers skill. The plan should:
1. Define the new service from scratch (using stored data, not yfinance)
2. Write tests first (TDD)
3. Implement
4. Delete stale nodes + recompute on clean data
5. Then continue 03-05 steps 5-8 (reconcile, re-test hypothesis, final verdict)

---
*HANDOFF-2026-04-17b.md*
