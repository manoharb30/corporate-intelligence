---
phase: 03-institutional-positioning
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - backend/app/main.py
  - backend/app/api/routes/ (delete 17 route files)
  - backend/app/services/ (delete unused services)
autonomous: true
---

<objective>
## Goal
Remove 17 unused backend routes and their services. Keep only the 7 routes the new dashboard needs.

## Purpose
Clean foundation before building new pages. No dead endpoints, no confusion about which API to use.

## Output
- main.py with 7 routes (was 24)
- Unused route files deleted
- Unused service files deleted
- Backend starts and all kept routes work
</objective>

<context>
@.paul/PROJECT.md
@docs/superpowers/specs/2026-04-17-dashboard-redesign-design.md
</context>

<acceptance_criteria>

## AC-1: Only 7 routes remain and backend starts clean
```gherkin
Given the backend cleanup is complete
When the backend starts with uvicorn
Then only these routes are registered: health, snapshot, event-detail, signal-performance, explorer, scanner, activist
And the backend starts without import errors
And all 7 kept routes respond to requests
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused routes from main.py and delete route + service files</name>
  <files>backend/app/main.py, backend/app/api/routes/*.py, backend/app/services/*.py</files>
  <action>
    **Step 1 — Remove 17 route registrations from main.py:**
    Remove include_router lines for: companies, persons, citations, events, feed, profile, insider_trades, stock_price, officers, alerts, accuracy, dashboard, anomalies, signal_context, company_intelligence, person_intelligence, signal_returns
    Remove their imports too.

    **Step 2 — Delete route files:**
    Delete from backend/app/api/routes/:
    companies.py, persons.py, citations.py, events.py, feed.py, profile.py, insider_trades.py, stock_price.py, officers.py, alerts.py, accuracy.py, dashboard.py, anomalies.py, signal_context.py, company_intelligence.py, person_intelligence.py, signal_returns.py

    **Step 3 — Delete unused service files:**
    Delete services that ONLY served deleted routes. Check each service for imports from kept routes before deleting.
    Likely to delete: feed_service.py, dashboard_service.py, dashboard_precompute_service.py, accuracy_service.py, anomaly_service.py, signal_context_service.py, company_intelligence_service.py (check if legacy version too), person_intelligence_service.py, stock_price_service.py, company_profile_service.py, event_detail_service.py (CHECK — event_detail route uses insider_cluster_service directly, not this)
    
    DO NOT delete:
    - insider_cluster_service.py (used by snapshot + event_detail)
    - snapshot_service.py (used by snapshot route)
    - signal_performance_service.py (used by signal-performance route)
    - explorer_service.py (used by explorer route)
    - activist_filing_service.py (used by activist route)
    - signal_filter.py (used by pipeline)
    - trade_classifier.py (used by pipeline)
    - Any service used by the scanner

    **Step 4 — Verify imports:**
    Check that main.py and all remaining route files have no broken imports.
  </action>
  <verify>
    cd backend && venv/bin/python -c "from app.main import app; print(f'Routes: {len(app.routes)}')"
    venv/bin/python -c "
    import asyncio
    from app.db.neo4j_client import Neo4jClient
    from app.services.snapshot_service import SnapshotService
    from app.services.insider_cluster_service import InsiderClusterService
    from app.services.signal_performance_service import SignalPerformanceService
    from app.services.explorer_service import ExplorerService
    print('All kept services import OK')
    "
  </verify>
  <done>AC-1 satisfied: 7 routes remain, backend starts clean, no broken imports</done>
</task>

</tasks>

<boundaries>
## DO NOT DELETE
- insider_cluster_service.py, snapshot_service.py, signal_performance_service.py
- explorer_service.py, activist_filing_service.py
- signal_filter.py, trade_classifier.py
- neo4j_client.py, any db/ files
- scanner/ directory (operational, not API-facing)
- Pipeline scripts (classify, merge, ingest, backfill)

## SCOPE LIMITS
- Backend cleanup only — no frontend changes
- Delete files, don't refactor kept services
</boundaries>

<verification>
- [ ] main.py has 7 include_router lines
- [ ] Backend starts without errors
- [ ] All kept route imports resolve
- [ ] No orphaned imports in main.py
</verification>

<success_criteria>
- 17 dead routes removed
- Backend is clean — only what the new dashboard needs
</success_criteria>

<output>
After completion, create `.paul/phases/03-institutional-positioning/03-02-SUMMARY.md`
</output>
