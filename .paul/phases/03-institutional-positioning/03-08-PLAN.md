---
phase: 03-institutional-positioning
plan: 08
type: execute
wave: 1
depends_on: ["03-07"]
files_modified:
  - backend/app/api/routes/event_detail.py
  - backend/app/services/ (delete 6 files)
  - frontend/src/services/api.ts
  - frontend/src/pages/SignalDetail.tsx
autonomous: false
---

<objective>
## Goal
Remove ~3,100 lines of dead code identified by the full cascading audit. Clean the event_detail route, delete 6 unused services, remove dead frontend types and unreachable code paths.

## Purpose
Dead code creates confusion, false dependencies, and maintenance burden. The product only shows strong_buy buy clusters — all 8-K, compound signal, LLM analysis, party linking, and sell signal code paths are dead weight.
</objective>

<context>
@.paul/PROJECT.md

## Audit findings
Full cascading audit traced from frontend API calls → backend routes → services.
6 services (3,143 lines) are not reachable from any frontend path.
Event_detail route has 4 dead endpoints.
Frontend has unused types and unreachable code.
</context>

<acceptance_criteria>

## AC-1: Dead services deleted
```gherkin
Given the 6 identified dead services
When they are deleted
Then no import errors exist in remaining code
And the backend starts clean
```

## AC-2: Event detail route cleaned
```gherkin
Given the event_detail route
When dead endpoints are removed
Then only the CLUSTER- path remains
And link-parties, deals, analysis, and COMPOUND- routing are gone
```

## AC-3: Frontend cleaned
```gherkin
Given the frontend code
When unused types and dead code paths are removed
Then api.ts has only types used by the 3 pages
And SignalDetail.tsx has no hardcoded false flags or unreachable branches
And TypeScript compiles with zero errors
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Delete 6 dead backend services</name>
  <files>
    backend/app/services/accuracy_service.py (DELETE),
    backend/app/services/company_profile_service.py (DELETE),
    backend/app/services/compound_signal_service.py (DELETE),
    backend/app/services/event_detail_service.py (DELETE),
    backend/app/services/llm_analysis_service.py (DELETE),
    backend/app/services/party_linker_service.py (DELETE)
  </files>
  <action>
    Delete these 6 files:
    - accuracy_service.py (789 lines)
    - company_profile_service.py (355 lines)
    - compound_signal_service.py (678 lines)
    - event_detail_service.py (800 lines)
    - llm_analysis_service.py (262 lines)
    - party_linker_service.py (259 lines)

    Total: 3,143 lines removed.

    Do NOT delete:
    - signal_filter.py (used by pipeline merge_classifications.py)
    - explorer_service.py (planned for cross-company feature)
    - activist_filing_service.py (used by scanner + activist route)
    - alert_service.py (used by scanner)
  </action>
  <verify>ls app/services/*.py — should have 12 files (was 18)</verify>
  <done>AC-1 partially satisfied: files deleted</done>
</task>

<task type="auto">
  <name>Task 2: Clean event_detail route</name>
  <files>backend/app/api/routes/event_detail.py</files>
  <action>
    Strip to CLUSTER- path only:

    1. Remove imports: AccuracyService, CompoundSignalService, EventDetailService, PartyLinkerService
    2. Remove endpoints: POST /link-parties, GET /deals/{cik}, GET /{accession}/analysis
    3. Remove COMPOUND- routing from GET /{accession_number}
    4. Remove 8-K fallback (EventDetailService.get_event_detail)
    5. Keep ONLY: GET /{accession_number} → InsiderClusterService.get_cluster_detail()

    The route should be ~15 lines, not 91.
  </action>
  <verify>
    python -c "from app.main import app; print('OK')" — no import errors
    curl localhost:8000/api/event-detail/CLUSTER-0002035989-2026-03-13 — returns data
  </verify>
  <done>AC-2 satisfied: only CLUSTER- path remains</done>
</task>

<task type="auto">
  <name>Task 3: Clean frontend dead code</name>
  <files>frontend/src/services/api.ts, frontend/src/pages/SignalDetail.tsx</files>
  <action>
    **api.ts:**
    - Remove healthApi (unused)
    - Remove SnapshotBuyStats type (unused)
    - Remove unused SnapshotSignal fields from type: signal_type, signal_level, signal_action, entry_price, current_price, status
    - Remove 'WATCH' | 'PASS' from signal_action type (only BUY exists)
    - Remove SignalPerfSummary type (unused)
    - Remove InsiderContextData type (unused — was used by deleted pages)
    - Remove EventTimelineEntry type (unused)
    - Remove DecisionCard type (unused — SignalDetail uses perf data now)
    - Keep: SnapshotSignal (used fields), WeeklySnapshot, ClusterBuyerDetail, ClusterDetail, EventDetailResponse, SignalPerf, snapshotApi, eventDetailApi, signalPerfApi

    **SignalDetail.tsx:**
    - Remove hasHostile = false and the hostile warning block (lines 79, 228-238) — will re-add properly when API supports it
    - Remove WATCH/PASS decision badge rendering if it references removed DecisionCard type
    - Clean any unused imports
  </action>
  <verify>
    npx tsc --noEmit — zero errors
    Check each page loads in browser
  </verify>
  <done>AC-3 satisfied: frontend clean, compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Removed 3,143 lines of dead backend code + cleaned frontend</what-built>
  <how-to-verify>
    1. Backend starts: venv/bin/uvicorn app.main:app --port 8000
    2. Frontend starts: cd frontend && npm run dev
    3. Signal List (/) — loads, signal cards work
    4. Signal Detail (click a card) — loads with buyers + SEC links
    5. Performance Tracker (/performance) — loads with table
    6. No console errors in browser
    7. Backend: only 12 services remain in app/services/
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT DELETE
- signal_filter.py (pipeline dependency)
- explorer_service.py (planned feature)
- activist_filing_service.py (scanner dependency)
- alert_service.py (scanner dependency)
- feed_service.py (transitive dependency for insider_cluster)
- Any scanner/ files

## SCOPE LIMITS
- Delete and clean only — no new features
- No backend logic changes
- No pipeline changes

</boundaries>

<verification>
- [ ] 6 service files deleted
- [ ] event_detail.py stripped to ~15 lines
- [ ] Backend starts with no import errors
- [ ] Frontend compiles with zero TS errors
- [ ] All 3 pages work in browser
- [ ] CLUSTER- signal detail still works
</verification>

<success_criteria>
- 3,143 lines of dead code removed
- No functionality loss (product works identically)
- Clean dependency chain: frontend → routes → services (no orphans)
</success_criteria>

<output>
After completion, create `.paul/phases/03-institutional-positioning/03-08-SUMMARY.md`
</output>
