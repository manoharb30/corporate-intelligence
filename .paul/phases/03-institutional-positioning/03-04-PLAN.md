---
phase: 03-institutional-positioning
plan: 04
type: execute
wave: 1
depends_on: ["03-03"]
files_modified:
  - backend/app/services/signal_filter.py
  - backend/filter_form4_p_only.py
  - backend/parse_form4_p_to_json.py
  - backend/ingest_genuine_p_to_neo4j.py
  - backend/run_month.py
  - backend/run_week.py
autonomous: false
---

<objective>
## Goal
Fix pipeline bottlenecks, then backfill Jun-Nov 2024 using Claude Code for classification instead of Anthropic API. Zero LLM cost, faster execution.

## Purpose
Need ~195 mature signals to reach statistical significance (p<0.05) on buy/mcap pattern. Current: 131 signals at p=0.112.

## Output
- Pipeline fixes: persistent yfinance cache, combined filter+parse, code quality fixes
- 6 months of properly classified data (Jun-Nov 2024)
- Zero Anthropic API cost for classification
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/03-institutional-positioning/03-03-SUMMARY.md
@docs/superpowers/specs/2026-04-17-dashboard-redesign-design.md

## Code review findings
- yfinance sync calls in merge: 60-90 min wasted (cache resets between days)
- Double SEC fetch: filter downloads then parse re-downloads same filing
- match_classification O(n²): should be dict lookup
- Ingest checks exists after deleting: redundant
- No 429 retry in filter: filing silently dropped
- Dead code in run_month.py: 70 lines unused
</context>

<acceptance_criteria>

## AC-1: Pipeline bottlenecks fixed
```gherkin
Given the pipeline code
When the fixes are applied
Then yfinance earnings cache persists across days (file-based cache)
And filter+parse combined into single SEC fetch
And match_classification uses dict lookup (O(1))
And ingest skips redundant exists-check after delete
And SEC 429 responses trigger retry with backoff
And dead code removed from run_month.py
```

## AC-2: Phase A completes for Jun-Nov 2024
```gherkin
Given the fixed pipeline
When Phase A (fetch → filter+parse → prefilter) runs for Jun-Nov 2024
Then each day produces _p_prefiltered.json and _p_llm_queue.json
And prefilter catches ~35% of transactions deterministically
```

## AC-3: Claude Code classifies LLM queue items
```gherkin
Given the _p_llm_queue.json files from Phase A
When Claude Code reads and classifies each item
Then each item gets classification (GENUINE / NOT_GENUINE / AMBIGUOUS) with reason
And results are written back to the queue JSON files
And merge step (Phase C) can consume them
```

## AC-4: Data ingested and performance recomputed
```gherkin
Given the classified data
When merge + ingest runs for Jun-Nov 2024
Then GENUINE transactions appear in Neo4j for those months
And SignalPerformance is recomputed
And mature strong_buy count is ~195+
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix pipeline bottlenecks and code quality</name>
  <files>
    backend/app/services/signal_filter.py,
    backend/filter_form4_p_only.py,
    backend/parse_form4_p_to_json.py,
    backend/ingest_genuine_p_to_neo4j.py,
    backend/run_month.py
  </files>
  <action>
    Fix the 6 issues from code review:

    **1. Persistent yfinance cache (signal_filter.py):**
    - Save _earnings_cache to a JSON file (e.g., /tmp/earnings_cache.json) after each new fetch
    - Load from file on startup if it exists and is <24h old
    - This persists across subprocess invocations (each day is a new process)
    - Same for _hostile_cache: save/load from /tmp/hostile_cache.json

    **2. SEC 429 retry (filter_form4_p_only.py):**
    - On HTTP 429 response: wait 5 seconds, retry up to 3 times
    - Log the retry attempt
    - Only skip filing after 3 failed retries

    **3. match_classification dict lookup (ingest_genuine_p_to_neo4j.py):**
    - Build dict: {(accession, total_value, insider): classification} once
    - Replace linear scan with dict.get()

    **4. Remove redundant exists-check (ingest_genuine_p_to_neo4j.py):**
    - After delete-all-for-date, skip the MATCH lookup in ingest_transaction
    - Go straight to MERGE/CREATE (the delete already cleared the way)

    **5. Dead code cleanup (run_month.py):**
    - Remove unused run_phase_a_for_day(), run_phase_b(), run_phase_c_for_day() functions
    - Fix SEC_HOLIDAYS_2025 variable name if misleading

    **6. Stale docstring fix (filter_form4_p_only.py, parse_form4_p_to_json.py):**
    - Update "2 workers" comment to match actual WORKERS = 8
  </action>
  <verify>
    cd backend && venv/bin/python -m pytest tests/ -v --tb=short 2>&1 | tail -20
    — all existing tests pass
    — signal_filter tests still pass (cache behavior unchanged for test mocks)
  </verify>
  <done>AC-1 satisfied: all 6 bottleneck/quality fixes applied, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase A only for Jun-Nov 2024</name>
  <files>none (runs existing scripts, skips Phase B)</files>
  <action>
    Run the pipeline for 6 months but STOP AFTER PREFILTER (skip batch_llm_classify.py):

    For each trading day in Jun-Nov 2024:
    1. fetch_form4_index.py --date YYYY-MM-DD (get daily index from SEC EDGAR)
    2. filter_form4_p_only.py --date YYYY-MM-DD (filter to P-only)
    3. parse_form4_p_to_json.py --date YYYY-MM-DD (parse XML)
    4. prefilter_p.py --date YYYY-MM-DD (run 19 rules → prefiltered.json + llm_queue.json)

    SKIP: batch_llm_classify.py (Claude Code will classify instead)
    SKIP: merge_classifications.py (runs after classification)
    SKIP: ingest_genuine_p_to_neo4j.py (runs after merge)

    This needs a modified run script or manual orchestration.
    Create a simple run_phase_a.py script that runs only steps 1-4 per day.
    Run in background with logging to /tmp/backfill_phase_a.log.
  </action>
  <verify>
    ls form4_index_202406*_p_llm_queue.json | wc -l — should have files for each trading day
    ls form4_index_202411*_p_llm_queue.json | wc -l — same for November
    Check a sample queue file: should have items with classification: null
  </verify>
  <done>AC-2 satisfied: Phase A complete, queue files ready for classification</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Phase A complete — review queue size before Claude Code classification</decision>
  <context>
    Report: total LLM queue items across all 130 days.
    Show sample items so user can see what needs classifying.
    Estimate time for Claude Code classification.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with Claude Code classification</name>
      <pros>Zero API cost, classification happens in this session</pros>
      <cons>May take time depending on queue size</cons>
    </option>
    <option id="pause">
      <name>Pause and review queue items first</name>
      <pros>Can spot-check quality before bulk classification</pros>
      <cons>Delays the backfill</cons>
    </option>
  </options>
  <resume-signal>Select approach after reviewing queue size</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Claude Code classifies LLM queue items</name>
  <files>backend/form4_index_*_p_llm_queue.json (read + write)</files>
  <action>
    For each _p_llm_queue.json file from Jun-Nov 2024:
    1. Read the queue JSON
    2. For each unclassified item:
       - Read the payload (footnotes, security_title, ownership_type, transaction details)
       - Apply the same classification logic as the CLASSIFIER_PROMPT in classify_p_with_prefilter.py
       - Classify as GENUINE, NOT_GENUINE, or AMBIGUOUS with reason
    3. Write classification + reason back to the item in the queue JSON
    4. Save the updated file

    Classification rules (from CLASSIFIER_PROMPT):
    - GENUINE: open-market purchase by an individual insider for investment
    - NOT_GENUINE: RSU vesting, DRIP, ESPP, private placement, fund commitment, entity buyer
    - AMBIGUOUS: unclear from available data, could be either

    Batch by reading multiple queue files at once for efficiency.
  </action>
  <verify>
    Check a sample classified queue file: items should have classification != null
    Count: total classified, GENUINE count, NOT_GENUINE count, AMBIGUOUS count
  </verify>
  <done>AC-3 satisfied: all queue items classified</done>
</task>

<task type="auto">
  <name>Task 4: Run Phase C (merge) + ingest for Jun-Nov 2024</name>
  <files>none (runs existing scripts)</files>
  <action>
    For each trading day in Jun-Nov 2024:
    1. merge_classifications.py --date YYYY-MM-DD
       (merges prefilter + classified queue → classified.json)
       (runs structured deal detector + earnings filter + hostile flag)
    2. ingest_genuine_p_to_neo4j.py --date YYYY-MM-DD
       (writes GENUINE + AMBIGUOUS + FILTERED to Neo4j)

    Then:
    3. Run backfill_signal_coverage.py for new cluster companies
    4. Recompute SignalPerformance: POST /api/signal-performance/compute?days=730
  </action>
  <verify>
    MATCH (t:InsiderTransaction) WHERE t.filing_date >= '2024-06-01' AND t.filing_date < '2024-12-01' AND t.classification = 'GENUINE' RETURN count(t)
    MATCH (sp:SignalPerformance) WHERE sp.conviction_tier = 'strong_buy' AND sp.is_mature = true RETURN count(sp) — should be ~195+
  </verify>
  <done>AC-4 satisfied: data ingested, performance recomputed</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Re-run statistical tests on expanded dataset</decision>
  <context>
    After backfill completes:
    - New total mature strong_buy count
    - Updated buy/mcap sweet spot HR and alpha
    - Updated p-value
    - Does the pattern cross p<0.05?
    
    Present comparison: before vs after backfill.
  </context>
  <options>
    <option id="significant">
      <name>Pattern is statistically significant — implement confidence tiers</name>
      <pros>Proven edge, ready for production</pros>
      <cons>None</cons>
    </option>
    <option id="not-significant">
      <name>Pattern did not reach significance — keep as informational</name>
      <pros>Honest, doesn't overfit</pros>
      <cons>No HR improvement</cons>
    </option>
  </options>
  <resume-signal>Review results and decide</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- classify_p_with_prefilter.py — prefilter rules unchanged
- signal_filter.py earnings logic — only add persistent cache, don't change the rule
- Cluster detection thresholds in insider_cluster_service.py

## SCOPE LIMITS
- Backfill Jun-Nov 2024 only
- Fix only the 6 reviewed issues — no feature additions
- Claude Code classification uses same rules as existing CLASSIFIER_PROMPT

</boundaries>

<verification>
- [ ] All 6 code fixes applied
- [ ] Existing tests pass
- [ ] Phase A complete for 130 trading days
- [ ] All LLM queue items classified by Claude Code
- [ ] Phase C (merge + ingest) complete
- [ ] SignalPerformance recomputed
- [ ] ~195+ mature strong_buy signals
- [ ] Statistical re-test completed
</verification>

<success_criteria>
- Zero Anthropic API cost for classification
- Pipeline runs 30-40% faster with fixes
- ~195+ mature signals for statistical testing
- Buy/mcap pattern verdict: significant or not
</success_criteria>

<output>
After completion, create `.paul/phases/03-institutional-positioning/03-04-SUMMARY.md`
</output>
