---
phase: 02-signal-quality
plan: 04
type: research
autonomous: true
---

<objective>
## Goal
Test whether "activist files BEFORE insider cluster" produces better 90d returns than "no activist" or "activist files AFTER". Read-only validation on 164 filtered strong_buy signals.
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/02-signal-quality/CONTEXT-2B.md
</context>

<acceptance_criteria>
## AC-1: Temporal hypothesis tested
```gherkin
Given the 164 filtered strong_buy signals (GENUINE, earn<=60d, midcap, 2+ buyers, $100K+)
When each signal's company is checked for ActivistFiling with filing_date before vs after the cluster date
Then signals are split into 3 groups (no activist, activist before, activist after)
And 90d hit rate + alpha vs SPY is computed for each group
And results are reported with sample sizes
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Test activist temporal hypothesis on 164 signals</name>
  <files>None modified (read-only query)</files>
  <action>
    Write and run a Python script that:
    1. Queries Neo4j for the 164 filtered strong_buy signals (GENUINE, midcap, 2+ buyers, $100K+, earn<=60d)
    2. For each signal, queries ActivistFiling where target_cik = company cik
    3. Classifies into 3 groups:
       - NO_ACTIVIST: no ActivistFiling exists for company
       - ACTIVIST_BEFORE: at least one ActivistFiling.filing_date < cluster max(transaction_date)
       - ACTIVIST_AFTER: ActivistFiling exists but all filing_dates >= cluster date
    4. Computes 90d forward return + SPY alpha for each signal
    5. Reports per group: sample size, hit rate, mean return, mean alpha
    6. Print clear comparison table

    Use existing price_series + SPY from yfinance.
    Use existing hist_earnings_cache for earn<=60d filter.
    Read-only — no writes to Neo4j or files.
  </action>
  <verify>Script runs. 3 groups reported with sample sizes, hit rates, and alpha. Total = 164.</verify>
  <done>AC-1 satisfied: temporal hypothesis tested, 3 groups compared</done>
</task>
</tasks>

<output>
Results inform next decision: build compound signal or not.
After completion, create compressed SUMMARY.md.
</output>
