---
phase: 02-signal-quality
plan: 05
type: research
autonomous: true
---

<objective>
## Goal
Attribute the ~30 signal failures to activist filing patterns. 4 scenarios tested in one pass.
</objective>

<context>
@.paul/phases/02-signal-quality/CONTEXT-2C.md
</context>

<acceptance_criteria>
## AC-1: Failure attribution complete
```gherkin
Given the ~30 losing signals from our 85 filtered strong_buy set
When each loser is cross-referenced with ActivistFiling data
Then 4 scenarios are tested and reported:
  - Stake reductions (exit signals)
  - Hostile purpose text
  - Deep dive on ACTIVIST_BEFORE losers (who, what stake, what happened)
  - Post-signal activist arrivals for NO_ACTIVIST losers
And patterns distinguishing losers from winners are documented
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Attribute failures across 4 activist scenarios</name>
  <files>None modified (read-only)</files>
  <action>
    Single Python script that:
    1. Gets the 85 filtered signals (same query as 02-04), split into winners/losers
    2. For EACH LOSER, queries all ActivistFiling for that company (by target_cik)
    3. Tests 4 scenarios:

    SCENARIO 1 — Stake reduction:
    - For losers with 2+ activist filings for same filer: check if % decreased
    - Compare: do winners with activist also show stake reductions? (control)

    SCENARIO 2 — Purpose text keywords:
    - For losers with activist: extract purpose_text, search for hostile keywords
    - Keywords: "proxy", "remove", "replace", "strategic alternatives", "inadequate",
      "underperform", "oppose", "withhold", "hostile", "unsolicited"
    - Compare hostile keyword frequency in losers vs winners

    SCENARIO 3 — Deep dive ACTIVIST_BEFORE losers (6 signals):
    - Print each: ticker, cluster_date, r90, activist filer, stake %, purpose_text excerpt

    SCENARIO 4 — Post-signal activist arrival:
    - For NO_ACTIVIST losers: check if ActivistFiling appeared within 180 days AFTER cluster
    - If so: activist was attracted by same undervaluation but stock kept falling

    Print clear summary per scenario.
  </action>
  <verify>Script runs. All 4 scenarios reported with sample sizes.</verify>
  <done>AC-1 satisfied: failure patterns documented across 4 scenarios</done>
</task>
</tasks>

<output>
Results inform whether activist data can serve as a WARNING filter.
Create compressed SUMMARY.md after.
</output>
