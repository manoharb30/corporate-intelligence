# PAUL Handoff

**Date:** 2026-04-21
**Status:** Clean pause before machine shutdown. Everything committed + pushed.

---

## READ THIS FIRST

Fresh Claude: you have no prior context. This document tells you everything.

**Project:** LookInsight — Insider Conviction Signal Platform
**Production:** https://ci.lookinsight.ai

---

## Current State

**Version:** 1.6.0 (completed) — all 6 milestones shipped
**Milestone:** None active. Ready to scope v1.7 (Neudata presentation pack).
**Phase:** None active.
**Plan:** None active.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Idle — ready for v1.7 scoping]
```

---

## What's in the DB right now (verified at handoff)

- **142 mature strong_buy** SignalPerformance nodes. Byte-immutable (v1.2).
  - 79 at `signal_level='high'` (3+ buyers)
  - 63 at `signal_level='medium'` (2 buyers)
- **57 immature** SignalPerformance nodes. Refreshed with latest prices (Apr 20 close).
- **198 total** SignalPerformance nodes, all `direction='buy' AND conviction_tier='strong_buy'`.
- **141/142 mature rows have `mcap_at_signal_true`** from SEC XBRL (1 unresolvable — GAM closed-end fund).
- **55/56 immature rows have inline-fetched `mcap_at_signal_true`** (v1.6 wiring).
- **All SignalPerformance nodes tagged** `methodology_version='v1.1'`.

## Headline numbers (unchanged throughout v1.2–v1.6)

**142 · 66.9% · +14.04% · +8.72% alpha.**

The v1.1 research brief (shipped to Citadel/Squarepoint/Final on 2026-04-19) cites 141 / 67.4% / +9.0% / p<0.001. That's the frozen publication snapshot — don't change it.

---

## What was shipped in the last session (2026-04-20 through 2026-04-21)

**Full milestones closed:**

| Version | Milestone | Tag | Commit | Outcome |
|---|---|---|---|---|
| v1.2 | Signal Integrity — matured immutability | `v1.2.0` | `df2bb8f` | Matured rows frozen on compute_all |
| v1.3 | Pipeline Simplification — strong_buy only | `v1.3.0` | `edf6a41` | 372 legacy rows deleted, sell-side removed |
| v1.4 | Signal Quality Audit | `v1.4.0` | `4dc1c22` | Ground-truth XBRL mcap + 22-filter Bonferroni audit (zero passed) |
| v1.5 | Signal Tier Extension — REJECTED | `v1.5.0` | `e63cbd5` | Small/large tiers investigated, failed p<0.05 Bonferroni |
| v1.6 | Forward-going mcap capture | `v1.6.0` | `5bb36b0` | `_compute_one` now fetches XBRL inline for new signals |

**Point fixes after v1.6 close:**

| Commit | What |
|---|---|
| `42bf05c` | fix(frontend): align conviction labels with backend signal_level. DNA no longer shows "High Conviction". Dashboard + SignalList both use signal_level from backend. Pushed to main. |
| `08588b7` | feat(backfill_signal_coverage): add `--refresh-older-than N` flag for freshness ops. Pushed to main. |

**Operational runs (not commits — DB state changes):**
- Refreshed `Company.price_series` + `Company.market_cap` for 543 cluster-signal companies (yfinance pull, 18.8 min). Matured SignalPerformance rows unaffected (v1.2 invariant).
- Ran `compute_all(days=760)` to refresh the 57 immature SignalPerformance rows with fresh prices.

## BETR example (illustrates the pipeline is working)

Pre-refresh (stale as of Apr 13): BETR price_current = $34.34, return so far +6.5%
Post-refresh (Apr 20 close): BETR price_current = **$46.33**, return so far **+43.7%**

Matured BETR-era signals: unchanged, as guaranteed by v1.2.

---

## Git state

```
Local branch: main
Last commit:  08588b7 feat(backfill_signal_coverage): add --refresh-older-than flag
Pushed to:    origin/main (up to date)
Tags local:   v1.0.0, v1.1.0, v1.2.0, v1.3.0, v1.4.0, v1.5.0, v1.6.0
Tags remote:  v1.0.0 + v1.1.0 only (the others are local-only).
              Push with: git push origin v1.2.0 v1.3.0 v1.4.0 v1.5.0 v1.6.0
              (Not blocking — nothing depends on remote tags.)
```

**Uncommitted tracked modifications** (all pre-existing before this session — not owned by recent work):
- `backend/exports/DATA_DICTIONARY.md`
- `backend/exports/export_signals_v1_1.py`
- `backend/requirements.txt`

Leave these alone unless the user addresses them intentionally.

**Cleanup bucket (still pending triage):**
Long list of untracked files under `backend/`, `.paul/phases/03-*` and `05-*` directories, root PDFs, Neo4j credential text files, `.design-review/`, `.jez/`, etc. Same bucket as the 2026-04-18 handoff. Not this session's problem.

---

## What the user is coming back to

**Imminent:** Neudata research team call (scheduled for "tomorrow" per 2026-04-20 discussion → that's today 2026-04-21).

**Planned v1.7 scope (not yet created):**
- Milestone: **v1.7 Neudata Research Presentation**
- Theme: A cohesive deliverable pack for the Neudata call + future scouts.
- Probable single phase + single plan. Deliverable: a "Methodology Update" PDF that sits alongside the v1.1 brief and documents v1.4-v1.6 work (SEC XBRL mcap, 22-filter Bonferroni audit, tier extension investigation).
- User's earlier preference: strong scientific framing (rigor + honest results) over pure marketing.

**v1.7 NOT YET CREATED.** No MILESTONE-CONTEXT.md, no ROADMAP entry, no phase dirs. Fresh Claude should run `/paul:discuss-milestone` to scope properly before building.

---

## Known open items (none urgent, user choices)

1. **Push the missing git tags** (v1.2-v1.6) — purely cosmetic; local tags are sufficient.
2. **Cleanup bucket triage** — same as before; user deferred repeatedly.
3. **Client-facing correction note** — deferred from v1.1; no immediate need.
4. **Extended coverage backfill** (Jan–Apr 2024, 2023) — flagged as next highest-leverage move for any future filter refinement. Would roughly double the signal pool.
5. **Classification swap** from `market_cap` (ratio estimate) to `mcap_at_signal_true` (XBRL) for immature signals — currently both populated, but `compute_conviction_tier` still reads the old one. User has chosen not to address this; current approach is defensible.

---

## What's frozen / must not change

- **v1.1 research brief** PDF: `.paul/phases/05-research-brief-pdf/LookInsight_Insider_Conviction_Signals.pdf`. Shipped to clients. Never regenerate with different numbers.
- **142 mature SignalPerformance rows**: v1.2 invariant. Only migrations can touch them.
- **Only strong_buy rows in SignalPerformance**: v1.3 invariant.
- **`methodology_version='v1.1'`** on all current rows until a tier is adopted (v1.5 explicitly rejected adding new tiers).

---

## Resume Instructions

When the user starts a new Claude session:

1. Read this file first.
2. Read `.paul/STATE.md` for loop position (should show idle / ready for v1.7).
3. If user starts by talking about Neudata → run `/paul:discuss-milestone` to scope v1.7.
4. If user asks "what's the state?" — direct them to headline numbers + this handoff summary.

---

## Resume file path

`.paul/HANDOFF-2026-04-21.md` (this document)

---

*Handoff created: 2026-04-21. Everything committed + pushed. Safe to shut down.*
