# v1.3 Pipeline Simplification — strong_buy only — Archive

**Archived:** 2026-04-20
**Status:** Complete

---

## Milestone: v1.3 Pipeline Simplification — strong_buy only

**Theme:** Strip legacy signal tiers (`buy`, `watch`) and the sell-side direction from the live pipeline. The product surface has been `strong_buy buy` only since v1.0; the machinery kept computing 266 mature rows the user never saw. Remove the dead code paths + legacy data.

## Phases

| Phase | Name | Plans | Status | Completed |
|-------|------|-------|--------|-----------|
| 8 | Strong_buy-only pipeline | 1/1 | ✅ Complete | 2026-04-20 |

### Phase 8: Strong_buy-only pipeline

**Focus:** (a) Remove sell detection from `compute_all`; (b) short-circuit `_compute_one` when `conviction_tier != 'strong_buy'`; (c) one-time DELETE of legacy rows (`NOT (direction='buy' AND conviction_tier='strong_buy')`); (d) cleanup in `snapshot_service`, signal_performance API route, and frontend type union; (e) regression tests.

Plans:
- **08-01** ✅ (execute) — strong_buy-only pipeline; 3 new tests; 372 legacy rows deleted; 142 matured strong_buy preserved byte-identically (v1.2 invariant upheld).

### Scope constraints

- Single concern only (no backfill, no parser work, no UI redesign).
- `InsiderClusterService` stays parameterized for sell — utility tests still exercise it; only live callers changed.
- `direction` + `conviction_tier` columns kept on SignalPerformance (future-proof; always `'buy'` / `'strong_buy'` now).
- Pure computation functions untouched.

### Pre-migration / post-migration DB state

| Stat | Before | After |
|---|---|---|
| SignalPerformance total | 570 | 198 |
| Mature (all tiers/directions) | 408 | 142 |
| Mature strong_buy buy (surfaced) | 142 | 142 (byte-identical) |
| Immature non-strong_buy | 106 | 0 |
| Mature non-strong_buy | 266 | 0 |

### Commit

`edf6a41` — feat(08-strong-buy-only): narrow pipeline to strong_buy buy only (v1.3 complete)

---

*Archived from active ROADMAP.md at milestone close.*
