# v1.6 Forward-going mcap capture — Archive

**Archived:** 2026-04-20
**Status:** Complete

---

## Milestone: v1.6 Forward-going mcap capture at signal creation

**Theme:** Close the remaining gap from v1.4. Historic signals got ground-truth mcap via the Phase 9 backfill script, but new signals from the daily scanner still used the price-ratio estimate. v1.6 wires the XBRL client into `_compute_one` so every new SignalPerformance node carries the correct mcap from creation.

## Phase

| Phase | Name | Plans | Status | Completed |
|---|---|---|---|---|
| 16 | Inline XBRL mcap at compute_all time | 1/1 | ✅ Complete | 2026-04-20 |

## Outcome

- `_compute_one` is now async and accepts an `xbrl_cache`.
- Per-CIK XBRL fetch with 1s pacing inside each compute_all run.
- Picks pre-signal shares-outstanding; falls back to ≤90d post-signal.
- Writes 6 provenance fields on the new SignalPerformance nodes.
- 55/56 immature rows populated on first recompute (1 unresolvable XBRL).
- Matured rows unchanged (v1.2 immutability preserved).
- Test suite at 74 (3 new for inline XBRL).

## Deliberate non-changes

- `estimate_historical_mcap` still populates the old `market_cap` field (backward compat).
- `compute_conviction_tier` still reads `market_cap` (ratio estimate) for classification — switching to `mcap_at_signal_true` is deferred beyond v1.6.

## Commit

`5bb36b0` — feat(16-inline-xbrl-mcap): forward-going mcap capture at compute_all (v1.6)

## Headline numbers (unchanged)

**142 mature strong_buy · 66.9% hit rate · +14.04% avg return · +8.72% avg alpha.**

## Next

The last genuinely "open" data-layer gap is now closed. Future work options (user decides):
- **Extended coverage backfill** (Jan–Apr 2024 + 2023) — still the highest-leverage move for statistical power.
- **Classification switch** from `market_cap` to `mcap_at_signal_true` — a methodology change; needs its own decision.
- **Client correction note** for v1.1 brief recipients.

---

*Archived from active ROADMAP.md at milestone close.*
