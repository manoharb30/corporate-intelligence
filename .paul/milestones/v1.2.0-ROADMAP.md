# v1.2 Signal Integrity — matured immutability — Archive

**Archived:** 2026-04-20
**Status:** Complete

---

## Milestone: v1.2 Signal Integrity — matured immutability

**Theme:** Lock matured-signal classifications so recompute is idempotent. Matured `SignalPerformance` rows are frozen historical records; `compute_all` must never touch them.

## Phases

| Phase | Name | Plans | Status | Completed |
|-------|------|-------|--------|-----------|
| 7 | mcap snapshot (matured immutability) | 1/1 | ✅ Complete | 2026-04-20 |

### Phase 7: mcap snapshot

**Focus (final implementation):** `compute_all` reads matured `signal_id`s before any DELETE; deletes only `is_mature = false OR NULL`; `_compute_one` short-circuits when `cluster.accession_number in mature_ids`; `_fetch_all_for_dashboard` feeds the full set (preserved + new) to `_save_dashboard_stats`.

Plans:
- **07-01** ✅ (execute) — matured-signal immutability invariant; 4 new tests; 408 matured rows preserved byte-identically in live verification.

### Notable scope shift

Initially planned as "add `market_cap_at_signal` snapshot column". During user-facing discussion, the simpler root-cause fix emerged: **never recompute matured rows**. The snapshot field became redundant and was dropped.

### Also completed during the same session

- May 2024 Form 4 backfill (first extended-coverage month).
- TZ-suffix fix in `signal_filter.py` (`signal_date[:10]` before `strptime`).
- `Before writing database queries` hard rule added to `CLAUDE.md`.
- Expanded `SignalPerformance` section + "Immutability invariant" subsection in `neo4j/schema-report.md`.

### Commit

`df2bb8f` — feat(07-mcap-snapshot): matured-signal immutability invariant (v1.2 complete)

---

*Archived from active ROADMAP.md at milestone close.*
