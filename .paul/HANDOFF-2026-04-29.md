# HANDOFF — 2026-04-29 (system shutdown, multi-day pause)

## TL;DR
Anthropic API quota exhausted mid-Jan-2023 backfill. Jan 4 + 5 + 6 done (mixed: 4-5 via pipeline, 6 via manual Claude.ai web classification). Jan 7-31 + Feb-Apr 2023 still to do. Resume when API quota resets (or continue manual via web for high-value days).

## Where the loop is paused

`run_week_historical.py --start 2023-01-04 --days 19` halted on **Jan 6, 2023** because all 21 LLM calls returned API_ERROR (quota cap). Pipeline preserves intermediate files — skip-if-exists will resume.

## What got ingested this session

| Day | Path | GENUINE ingested | Clusters | Mcap fill |
|-----|------|------------------|----------|-----------|
| 2023-01-04 | run_week_historical pipeline | yes (via auto LLM) | 2 created | 8/10 (today's mcap, 4-5 missing in 2023) |
| 2023-01-05 | run_week_historical pipeline | yes (via auto LLM) | 0 | 6/8 (today's mcap) |
| 2023-01-06 | manual: Claude.ai web → merge → ingest_genuine_p_historical | 2 surviving (5 LLM-GENUINE, 3 dropped by earnings filter) + 1 AMBIGUOUS | 0 | 0 historical / step 4 (today's-mcap) NOT YET RUN |

Clusters across these 3 days: **2 total**, both on Jan 4. (Same companies re-traded Jan 5/6 mostly — none crossed the 2-buyer threshold.)

## How Jan 6 was salvaged (workflow for future API-blocked days)

1. The pipeline left `form4_index_20230106_p_llm_queue.json` with 21 unclassified items.
2. Uploaded the queue file to **Claude.ai web** with the classifier rubric (copied from `classify_p_with_prefilter.py:CLASSIFIER_PROMPT`).
3. Claude.ai returned the same JSON with `classification` + `reason` filled.
4. Saved the response, copied it over the queue file: `cp _p_classified.json _p_llm_queue.json`.
5. Ran `merge_classifications.py --date 2023-01-06` → wrote real `_p_classified.json`.
6. Ran `ingest_genuine_p_historical.py --date 2023-01-06`.

This gives a way to keep the backfill moving without the API.

## Open / pending

- **Step 4 still pending for Jan 6**: `fill_current_mcap_for_day.py --date 2023-01-06` (5 unique CIKs). Cheap, ~30s.
- **Jan 7-31, 2023**: 18 trading days. Each day has 200-700 P transactions, ~20 LLM-bound after prefilter. Either resume `run_week_historical.py --start 2023-01-07 --days 18` after API quota resets, OR drag each `_p_llm_queue.json` through Claude.ai web.
- **Feb 2023 → Apr 2024**: long backfill arc. Jan 4 → Apr 30 2024 already done in earlier sessions.
- **Maturities to flip when 90d hits** (do NOT touch before):
  - USAR — matures 2026-04-29 (today, but pipeline doesn't auto-run; user has to trigger)
  - AMTB, WS — mature 2026-04-30

## State of code (uncommitted, do not commit without user OK)

- `backend/run_week_historical.py` — 2023 SEC holidays added (Jan 2/16, Feb 20, etc.) so MLK day didn't crash the pipeline. **Untracked**.
- `backend/ingest_genuine_p_historical.py` — historical-mcap variant of the live ingest. **Untracked**.
- `backend/fill_current_mcap_for_day.py` — fills today's mcap for any single filing date. **Untracked**.
- `backend/refresh_immature_current_price.py` — already ran successfully on 32 immature SP rows; updates `price_current` only. **Untracked**.
- `frontend/src/pages/SignalList.tsx` — hero numbers updated (51,054 / 8,357 / 2,287). **Modified**.
- `CLAUDE.md` — HARD RULES section added at top. **Modified**.
- `backend/app/services/insider_cluster_service.py` — `process_incremental` + helpers. **Modified**.
- `backend/merge_classifications.py` — structured-detector threshold lowered to ≥3 insiders. **Modified**.

## Strict behavioral rules (re-loaded by `feedback_*.md` memory)

- `feedback_no_auto_actions.md` — never run any mutating shell command without an explicit "yes/run/do" for THAT specific action. "Want me to X?" is a question, not permission.
- `feedback_precise_answers_only.md` — 1-3 sentences default. No long stories.
- `cluster_window_rule.md` — 30d cluster window is FROZEN at formation. 90d is for return calc only.
- `feedback_matured_signals_frozen.md` — 142-row historic anchor is frozen. Don't propose retroactive recompute.
- `feedback_no_naming_funds.md` — no buyside firm names in external artifacts without written approval.

## Open investigations (not started; user said "later")

- **CODI Jan 18 2024 — 11 insiders at $21.40**: stored as GENUINE not AMBIGUOUS. Structured detector should have caught it (≥5 insiders at same price/date). Investigate why the threshold didn't fire on this row. Memory: `known_issue_structured_detector_missed_codi.md`.

## Numbers as of close of session (verified, do not quote externally without re-querying)

- Mature strong_buys: **162** (142 anchor + 20 backfill Jan-Apr 2024)
- HR / alpha: 67.9% / +8.8pp / 62.3% beat-SPY (cohort blend)
- Immature strong_buys: 32 (price_current refreshed Apr 27)
- DashboardStats: total_signals=162, year=2024 only shows 42 (20 backfill + 22 anchor portion in 2024)

## When you (Claude) resume

Read this handoff first, then `STATE.md`, then check `MEMORY.md` for the new strict rules.
