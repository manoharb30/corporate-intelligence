# PAUL Handoff — 2026-04-23

**Date:** 2026-04-23 early morning (session ran 2026-04-22 evening into 2026-04-23)
**Status:** Paused — user (Manohar) stopped because the session had accumulated too much confusion and he wants to restart fresh.
**Tone for fresh Claude:** Present facts. Do not defend prior analyses. Mark what is verified vs inferred. User is tired and suspicious of shifting numbers — be explicit about ground truth.

---

## READ THIS FIRST

The session produced two real outcomes (code fix + DB state change) and one accumulation of confusion (my analysis quality was uneven — I invented methodology in one analysis, mis-framed a number to the point of undermining user trust, and never fully answered "why did 23 old-dated signals surface in compute_all today"). Separate the real outcomes from my interpretations.

**Anchors that will NOT change:**
- The 142 mature strong_buy rows are frozen. Rules that produced them stay. No retroactive re-classification anywhere. Memory: `feedback_matured_signals_frozen.md`.
- Earnings-proximity rule (≤60d to next earnings) applies to unmatured signals only (Phase 17 Option 1 decision). Matured 142 keep their original classification.
- v1.2 immutability invariant — `compute_all` never touches matured SignalPerformance rows.

---

## Current State (verified from code, DB, logs)

### PAUL state
- Version: 1.7.0 in progress
- Milestone: v1.7 Signal Pipeline Reconciliation (created 2026-04-23)
- Phase 17 — **Complete** (decision-only plan, Option 1 chosen)
- Phase 18 — **Plan APPLY complete, UNIFY not started**
- Phases 19 + 20 — Not started

### Code state — `backend/app/services/insider_cluster_service.py`
Four edits applied, uncommitted:
1. Line 218: `AND t.classification = 'GENUINE'` added to `detect_clusters` Cypher
2. Line 281: `and not t.get("is_10b5_1", False)` added to buy branch of `target_trades` (symmetric with sell branch at line 274)
3. Line 423: `in_sweet_spot = 300_000_000 <= mcap <= 5_000_000_000` (was `10_000_000_000`). Comment at lines 407-411 updated.
4. Lines 438-442: `except Exception as e:` + `logger.warning(...)` replacing silent fallback.

Verify current state with:
```
grep -n "t.classification = 'GENUINE'\|5_000_000_000\|not t.get(\"is_10b5_1\", False)\|StockPriceService.get_market_cap failed" backend/app/services/insider_cluster_service.py
```

### DB state after compute_all ran post-fix
- `preserved_mature: 142` (unchanged — v1.2 invariant held)
- `computed: 55` (new SP rows this run)
  - 23 of them have `is_mature = True`
  - 32 have `is_mature = False`
- Total SignalPerformance rows in DB: 197
- Cross-check (`audit_overrides_2026-04-22.py`): zero non-GENUINE-backed SP rows; 4 AEVEX overrides carry `classification = NOT_GENUINE` and are excluded from clusters

Log: `/tmp/compute_all_post_fix.log`

### AEVEX overrides in DB
4 rows, all `classification = NOT_GENUINE`:
- Hush Brett P — filed 2026-04-20, `ipo_allocation_2026-04-21`
- Jackson Michael Andrew — filed 2026-04-20, `ipo_allocation_2026-04-21`
- Wells Charles Roger IV — filed 2026-04-20, `ipo_allocation_2026-04-21`
- Booth Todd — filed 2026-04-21, `ipo_allocation_2026-04-22`

### Data counts (all-time P-transactions)
- NULL classification: 8,761 (legacy, pre-classification pipeline)
- GENUINE: 7,578
- FILTERED: 6,324 (demoted by earnings-proximity rule)
- AMBIGUOUS: 313
- NOT_GENUINE: 4 (all AEVEX)

---

## What Was Done in the Session

### Evening 2026-04-22 → Early morning 2026-04-23

1. **142-mature audit** — Re-fetched yfinance + SEC XBRL for every mature strong_buy. Returns matched fresh yfinance 140/142 within ±0.5pp, SPY 142/142. Headline +9pp alpha is verified correct against fresh source data. 9 of the 142 are outside the true midcap band (6 microcap, 3 largecap) per XBRL — but 142 is frozen, no retroactive change. **Output: `backend/audit_142_mature_2026-04-22.py` + `audit_142_mature_2026-04-22.json`.**

2. **Apr 21 Form 4 ingest** — Ran `venv/bin/python run_week.py --start 2026-04-21 --days 1`. 7 GENUINE P transactions written. Tickers: CRT (2), AVEX (1, later overridden), BMI (1), PODC (3). Total time 63 seconds.

3. **AEVEX override** — Booth Todd's Apr 21 filing flipped `GENUINE → NOT_GENUINE` via `backend/override_aevex_booth_2026-04-22.py`. Tagged `ipo_allocation_2026-04-22`. Completes the DSP-allocation override pattern that began yesterday for the other 3 officers.

4. **Price + mcap refresh** — Ran `backfill_signal_coverage.py --apply --refresh-older-than 1`. Processed 548 companies. 534 got mcap written, 547 got prices written, 1 failed.
   - From pre-run breakdown: `Missing market_cap: 14`, `Missing price_series: 0`, `Stale (refresh mode): 534`
   - After-run: 14 companies still have no mcap (yfinance returned None for those specific tickers)

5. **CRT enrichment** — CRT had no ticker / no price_series / no mcap. Enriched inline via `backend/enrich_crt_2026-04-22.py`. Now has $63.75M mcap + 502 price points.

6. **compute_all (first post-ingest run)** — Produced 142 mature preserved + 58 immature strong_buy. But 17 of 58 were contaminated: 16 FILTERED-backed (including BMI) + 1 NOT_GENUINE-backed (AEVEX re-surfaced because cluster detection ignored the override flip).

7. **Root bug found** — `InsiderClusterService.detect_clusters` (line 214-234) had no `classification` filter on its Cypher query. The `classification` tag set by the pipeline was being ignored. Documented in the session as the explanation for why the AEVEX override didn't stick + why 17 immature signals were FILTERED-backed + why 47 of 141 v1.1 brief signals fail earnings-proximity.

8. **v1.7 milestone created** via `/paul:discuss-milestone` + `/paul:milestone`. 4 phases planned: 17 (decision), 18 (correctness fixes), 19 (unification), 20 (mcap reconciliation).

9. **Phase 17 closed** with decision: earnings-proximity applies to unmatured signals only. Matured 142 stay as-is. No code change in Phase 17. Decision recorded in PROJECT.md + phase summary.

10. **Phase 18 applied** — Four edits to `insider_cluster_service.py` (listed above). compute_all rerun:
    - 142 preserved mature (v1.2 held)
    - 23 new mature + 32 new immature = 55 computed
    - 17 contaminated rows gone (AVEX, BMI, VSCO, MTN, MLAB, TPC, GPK, SKWD, ACIW, CMTG, TNC, SXC, ASST, QDEL, LUCK, MMS, RAL)
    - Pre-existing test failures (4 in `test_insider_cluster_service.py` + 1 in `test_company_search.py`) confirmed to NOT be caused by the edits — ran the same failing tests against stashed unedited code, got same failures. They are pre-existing and should be repaired in Phase 19.

### Contentious / unresolved items (fresh look warranted)

**(A) The 23 newly-surfaced matured signals.** 23 SP rows appeared in compute_all output flagged `is_mature = True`, with signal_dates from 2024-09 to 2025-12 — dates months before today. They were absent from the 142 preserved-mature set, meaning prior compute_all runs never stored them. Tickers + alphas listed below.

My explanations in the session were inconsistent:
- First I called them "newly matured" — wrong wording; they matured by date long ago
- Then "blocked by missing price/mcap data — today's refresh unblocked them"
- Then user pushed back: 30-day cluster windows mean today's data can't retro-form clusters. Correct — the cluster was formed at the original signal_date. What was missing was downstream scoring
- Then user pushed on the 548-vs-14 framing and correctly identified that my severity-framing shifted

What is actually **verified**:
- These 23 signals exist in SP now with `is_mature = True`
- Their `computed_at >= 2026-04-23`
- Their underlying clusters formed within their respective 30-day windows at signal time (cluster detection rules didn't change)
- `_compute_one` in `signal_performance_service.py` can silently drop clusters (returns None) when:
  - `not series` (line 299-300)
  - `0 not in delay_prices` (line 310-311 — day-0 price not found in series)
  - conviction_tier != 'strong_buy' (line 342-344)

What is **NOT verified**:
- For each of the 23, which specific mechanism blocked scoring in prior runs. This would require reconstructing each company's pre-today state (price_series coverage dates, market_cap value) and re-running `_compute_one` against that snapshot. I did not do this.

The 23 signals, sorted by signal date descending:

```
ticker  sig_date    act_date    n_ins  total_val    ret       spy      alpha
QDEL    2025-12-10  2025-12-10   4     $555,557     -32.99%   -1.22%   -31.77pp
LUCK    2025-12-08  2025-12-09   3     $282,838     -1.26%    -0.40%   -0.86pp
GSHD    2025-11-28  2025-12-01   2     $512,501     -26.77%   +1.20%   -27.97pp
RPD     2025-11-24  2025-11-24   2     $227,990     -56.90%   +2.34%   -59.24pp
PEBO    2025-11-21  2025-11-25   4     $118,828     +10.01%   +1.39%   +8.62pp
BBWI    2025-11-21  2025-11-24   3     $450,357     +45.95%   +2.34%   +43.61pp
EVLV    2025-11-21  2025-11-24   3     $410,602     -20.97%   +2.34%   -23.31pp
GEF     2025-11-20  2025-11-21   3     $2,047,221   +19.02%   +4.17%   +14.85pp
ACVA    2025-11-17  2025-11-18   2     $625,596     -1.68%    +3.76%   -5.44pp
SG      2025-11-12  2025-11-12   2     $1,099,492   +0.74%    +1.58%   -0.84pp
MTDR    2025-11-06  2025-11-06   4     $435,186     +24.99%   +2.67%   +22.32pp
CBK     2025-10-03  2025-10-15   2     $299,976     -0.78%    +4.61%   -5.39pp
FRPT    2025-09-15  2025-09-16   6     $332,305     +17.64%   +3.43%   +14.21pp
KRO     2025-08-13  2025-08-14   3     $117,220     -20.74%   +6.25%   -26.99pp
ICFI    2025-06-09  2025-06-10   2     $189,775     +11.60%   +7.90%   +3.70pp
MG      2025-03-20  2025-03-20   2     $298,269     -24.78%   +5.97%   -30.75pp
FLNC    2025-03-14  2025-03-17   2     $103,774     -7.39%    +6.58%   -13.97pp
CMTG    2025-03-13  2025-03-14   2     $567,787     -22.56%   +7.60%   -30.16pp
RC      2025-03-12  2025-03-14   6     $703,810     -8.35%    +7.60%   -15.95pp
BFC     2025-03-04  2025-03-19   2     $142,244     +11.11%   +5.68%   +5.43pp
ACDC    2025-02-26  2025-02-28   2     $4,348,658   +10.29%   -0.40%   +10.69pp
TCNNF   2024-11-21  2024-11-21  10     $422,540     -28.59%   +3.59%   -32.18pp
LMB     2024-09-12  2024-09-13   2     $157,295     +31.27%   +7.86%   +23.41pp
```

Aggregate: 9 winners, 14 losers, HR ~39%, avg alpha ~-6.7pp. Weak cohort compared to the 142's 67% / +8.7pp.

**These 23 are now locked as mature per v1.2 invariant and the frozen-signals rule.** A full mature cohort including them is 165 rows with blended HR ~63% and alpha ~+6.6pp. Whether you include them in any new external artifact is a product decision, not a code decision.

**(B) The 14 vs 548 framing.** I wrote "548 processed in 20.5 min" and later "only 14 real gaps, 534 just stale" — these are both factually defensible given the log, but the *implication* shifted. Stale price_series can block scoring even when the field isn't NULL (if the stored series doesn't extend to recent dates). Precise breakdown from the log:
- 548 companies processed by `backfill_signal_coverage.py --apply --refresh-older-than 1`
- 534 were in category "stale (refresh mode)" — price_series existed, price_series and mcap timestamps older than 1 day
- 14 were in category "Missing market_cap" — mcap field was NULL entirely
- 0 were in category "Missing price_series"
- After run: 547 had prices written, 534 had mcap written, 14 still have NULL mcap (yfinance returned None)

Neither "548 companies with gaps" nor "14 real gaps" is complete. The operative fact is that **enrichment is batch/periodic and runs separately from ingest, so new companies and companies with stale series accumulate silent-drop potential between batches.** `daily_pipeline_gaps.md` Gap 1 + Gap 2.

**(C) Pre-existing test failures.** 4 tests fail on unmodified code (verified via `git stash`):
- `test_company_search::test_combined_search_graph_only` — imports nonexistent `app.services.company_service` module
- `test_insider_cluster_service::TestOfficerPromotion::test_two_officers_promoted_to_high` — asserts `"Officer Cluster"` in signal_summary (string exists only in `get_cluster_detail`, not `detect_clusters`)
- `test_insider_cluster_service::TestConvictionTiers::test_three_buyers_two_officers_strong_buy` — asserts `strong_buy`, gets `buy`
- `test_insider_cluster_service::TestConvictionTiers::test_two_buyers_no_officers_watch` — asserts `watch`, gets `buy`

These tests were broken before Phase 18. They reflect old code expectations that no longer match production. Phase 19 (unification refactor) is the natural place to repair them.

**(D) Invalid analysis to discard.** `backend/hypothesis_full_cohort_2026-04-22.py` output is not trustworthy. It used same-day grouping instead of the production 30-day sliding window — that was my invention, not the actual clustering rule. The "+2.5pp on wider cohort" number is an artifact of the invented rule, not a property of the signal cohort. Discard that script's findings.

Trustworthy scripts from the session:
- `audit_142_mature_2026-04-22.py` / `.json` — verified 142-mature returns against fresh yfinance + XBRL
- `hypothesis_no_earnings_2026-04-22.py` — 142-cohort scenarios using the actual stored data
- `audit_overrides_2026-04-22.py` — survey of `classification_override` rows
- `override_aevex_booth_2026-04-22.py` — one-off data correction
- `enrich_crt_2026-04-22.py` — one-off data correction
- `run_compute_all_2026-04-22.py` — thin wrapper for `SignalPerformanceService.compute_all()`

---

## Uncommitted / Untracked Work

Nothing from this session is in git. Listed so nothing is at risk:

Modified, not committed:
- `backend/app/services/insider_cluster_service.py` (Phase 18 edits)
- `backend/backfill_daily.py` (2 dead imports removed earlier in session)
- `.paul/PROJECT.md`, `.paul/STATE.md`, `.paul/ROADMAP.md` (v1.7 milestone + Phase 17 decision)
- `backend/exports/DATA_DICTIONARY.md`, `backend/exports/export_signals_v1_1.py`, `backend/requirements.txt` (from prior sessions)

Untracked .py files in `backend/` (all this session):
- `audit_142_mature_2026-04-22.py` + `audit_142_mature_2026-04-22.json`
- `override_aevex_booth_2026-04-22.py`
- `enrich_crt_2026-04-22.py`
- `run_compute_all_2026-04-22.py`
- `hypothesis_no_earnings_2026-04-22.py`
- `hypothesis_full_cohort_2026-04-22.py` **(discard findings)**
- `audit_overrides_2026-04-22.py`

Untracked .py files in `backend/` from prior sessions:
- Many — see `git status`. Not this session's business.

Untracked PAUL structure:
- `.paul/HANDOFF-2026-04-22.md` (yesterday's handoff, not yet archived)
- `.paul/phases/17-methodology-decision/17-01-SUMMARY.md`
- `.paul/phases/18-cluster-correctness/18-01-PLAN.md` + `18-01-SUMMARY.md`
- `.paul/HANDOFF-2026-04-23.md` (this file)

Memory added this session:
- `feedback_read_memory_first.md`
- `feedback_matured_signals_frozen.md`

---

## Open Questions for the Fresh Look

You (Manohar) said you want to look at this from scratch. Questions that would benefit from a fresh frame:

1. **Do you accept the 23 newly-surfaced signals as legitimate mature strong_buys?** They're now locked by v1.2 + frozen-signals rule. If accepted, the mature cohort is 165 with blended numbers (~63% HR, ~+6.6pp alpha). If not accepted, there needs to be a rule about what "acceptance" even means — the matured-immutability invariant says you don't touch them. The only real option is: use the 23 or don't use them in external artifacts. They exist in the DB either way.

2. **Does Phase 18 close cleanly or does it need to be reopened?** The code edits did their job — AEVEX + 16 FILTERED-backed signals are gone, matured 142 preserved. But the session uncovered a broader question (silent drops in enrichment, 23 surfacing unexpectedly) that Phase 18 didn't scope. You may want:
   - Close 18 as-is, spin up a Phase 18.5 or Phase 21 for the enrichment-silent-drop issue
   - Or reopen 18 to include a per-signal audit of the 23 before moving on
   - Or accept 23 and move on

3. **Is the enrichment pipeline trustworthy enough for daily runs?** The 23 surfacing is evidence that enrichment + classification + clustering are not atomically consistent day-to-day. Gap 1 + Gap 2 from `daily_pipeline_gaps.md` are real and not scoped anywhere yet. Could become Phase 21 of v1.7, or a separate milestone.

4. **Phases 19 + 20 scope** — still as originally planned (unify cluster/detail conviction-tier + extract helper + mcap source reconciliation), or re-scope given what we've learned?

5. **Neudata timing** — still open. Current matured headline as-of-today is either 142 (frozen + clean per stated methodology) or 165 (frozen + 23 newly-surfaced). Neither number is "wrong"; the difference is whether you count the 23 that just appeared.

6. **Git hygiene** — the amount of untracked work is large. Before any more APPLY work, at minimum commit the state in a WIP commit so nothing is at risk.

---

## Fresh-Session Resume Instructions

1. When the user returns, do not re-explain the session. They've read this file.
2. Start with: "What do you want to tackle first?" — do not presume.
3. Expected priorities in rough order (user's call, not mine):
   - (a) Commit today's work (WIP commit) so nothing is lost
   - (b) Audit the 23 newly-surfaced signals per-signal to understand silent-drop mechanism (optional — answers open Q2 and Q3)
   - (c) Close Phase 18 via UNIFY or reopen it
   - (d) Proceed to Phase 19 / 20 planning
   - (e) Decide Neudata timing given current cohort state
4. Read memory BEFORE proposing anything. `feedback_read_memory_first.md` + `feedback_matured_signals_frozen.md` are in effect.
5. Do not run any new compute_all, backfill, or override without explicit user approval. The DB has been mutated enough this session.

---

*Handoff created 2026-04-23 early morning. Session ran ~12 hours across the 2026-04-22 → 2026-04-23 boundary. Keep this file until the fresh-session follow-up resolves the open questions, then archive to `.paul/handoffs/archive/`.*
