# HANDOFF — 2026-05-13 v2 (power-shutdown #2: ingest catchup + mcap-gate design)

## TL;DR

Caught up daily Form 4 ingest for **May 11–12** via `python run_week.py --start 2026-05-11 --days 2`. Pipeline ran cleanly: **17 GENUINE P-tx** across the two days (7 on May 11, 10 on May 12). **No new strong_buy signals** — only LZ ($1.05B) was midcap but had a single buyer; ACON (3 insiders, $7.6M) fails midcap floor; MKTW ($45.8M) microcap. Detect_clusters returned 2 weak clusters (ACON HIGH-tier=watch, AXIA3 MEDIUM-tier=buy with missing mcap — Brazilian foreign listing).

After ingest, user opened a **token-saving design question**: gate microcaps BEFORE LLM classification to skip junk filings entirely. User's decision: **discard, don't ingest** (no SKIPPED_MICROCAP bucket, no fallback). Open sub-question paused mid-discussion: how to handle filings where mcap is unknown (new CIK with no Company node).

Power shutdown hit before answering.

## What got done this session

| Step | What | Where |
|---|---|---|
| 1 | Resumed PAUL context, read HANDOFF-2026-05-13.md | resume flow |
| 2 | Launched `run_week.py --start 2026-05-11 --days 2` in background | `backend/logs/run_week_2026-05-11_2d.log` + `/tmp/run_week_20260511_20260512.log` |
| 3 | Pipeline completed in 7.2 min: 289 Form 4s → 17 GENUINE | totals: May 11: 155→7 GEN; May 12: 134→10 GEN |
| 4 | Wrote read-only check script + ran it | `backend/check_may11_12_clusters.py` |
| 5 | Confirmed no new strong_buy signals from the new tx | result table below |
| 6 | Discussed pre-LLM mcap gate design (3 options) | conversation only — NOT implemented |

## Numbers (verified)

**GENUINE P-tx ingested May 11–12 (5 rows, 3 companies):**
- LZ (LegalZoom) — 1 insider, $769K, mcap $1.05B → midcap but single-buyer (no cluster)
- ACON (Aclarion) — 3 insiders, $46K, mcap $7.6M → cluster forms but fails midcap floor
- MKTW (MarketWise) — 1 insider, $189K, mcap $45.8M → microcap + single buyer

**detect_clusters(days=5) returned 2 buy clusters — neither strong_buy:**
- ACON: HIGH level, 3 buyers, $46K, tier=watch (mcap $7.6M < $300M)
- AXIA3 (AXIA Energia S.A.): MEDIUM, 2 buyers, $576K, tier=buy, mcap MISSING (foreign listing, yfinance 404)

**Conclusion:** Nothing to add to SignalPerformance from these two days.

## Open design decision (POWER SHUTDOWN PAUSED HERE)

**User intent:** add an mcap gate BEFORE `batch_llm_classify.py` to skip microcap filings entirely. No SKIPPED_MICROCAP bucket — just discard.

**Proposed shape (not implemented):**
```
prefilter_p.py → mcap_gate.py → batch_llm_classify.py → merge → ingest
                      │
                      └── DROP (no ingest, no record)
```

**Floor:** $300M (midcap minimum). User did not address ceiling ($5B) or padding.

**Unresolved sub-question (the actual pause point):** for filings whose CIK has no `Company` node yet (~5–10% of daily filings — known gap per `daily_pipeline_gaps.md`), how do we gate?

1. **Discard** (aggressive — risk losing newly-IPO'd midcap before enrichment)
2. **Enrich inline** — one yfinance call per unknown ticker, then gate (~1s/CIK, no false discards)
3. **Fail open → classify → discard post-LLM if microcap** (partial token savings)

User did not answer before pausing.

## State of code (uncommitted)

This session added/modified:
- `backend/check_may11_12_clusters.py` — new, read-only audit (May 11–12 GENUINE tx + cluster check + mcap coverage)
- `backend/logs/run_week_2026-05-11_2d.log` — new, top-level run output (24 lines, redacted; full step logs in /tmp/)

Intermediate files for May 11–12 written to `backend/`:
- `form4_index_20260511_p_prefiltered.json`, `_llm_queue.json`, `_parsed.json`, `_classified.json`
- `form4_index_20260512_p_prefiltered.json`, `_llm_queue.json`, `_parsed.json`, `_classified.json`

**Carryover untracked work (NOT touched this session)** — from prior handoffs:
- `backend/check_price_freshness.py`, `backend/refresh_immature_current_price.py` (modified)
- `backend/check_tecx_bmi_state.py`, `backend/fix_tecx_bmi_returns.py`, `backend/check_cohort_aggregate.py`, `backend/check_avbc_may8.py`, `backend/refresh_snapshot_blobs.py`
- `backend/run_week_historical.py`, `backend/ingest_genuine_p_historical.py`, `backend/fill_current_mcap_for_day.py`, `backend/flip_3_to_mature.py`, `backend/invalidate_codi_2023_01_05.py`, `backend/merge_classifications.py`
- Modified: `backend/app/services/signal_performance_service.py` (compute_all guard from 2026-05-12), `backend/app/services/insider_cluster_service.py`, `backend/run_week_historical.py`, `frontend/src/pages/SignalList.tsx`, `CLAUDE.md`

## Still pending from prior handoffs (UNCHANGED)

- **Phase 18 UNIFY** still pending (PLAN 18-01 APPLY ✓, UNIFY ○).
- **Phase 19** (unified `refresh_prices.py`) — direction agreed, no PLAN drafted.
- **Phase 20** (remove SnapshotBlob, dashboard reads SP directly) — direction agreed, no PLAN drafted. Sequencing open (fold vs split).
- **`Company.price_series` stale for 19 tickers** (last=2026-04-22). Signal-detail price charts flat past Apr 22 for those tickers.
- **Daily Form 4 ingest** now caught up through May 12. May 13 not yet ingestable (US still nighttime when user paused).
- **BETR + TBN** have no Company node — orphaned SP rows.
- **Balyasny + Patient Square outreach drafts** in user's hands, unconfirmed sent.
- **NX1 fund** context still unresolved.

## When you (Claude) resume

Read this handoff (HANDOFF-2026-05-13-v2.md), then prior HANDOFF-2026-05-13.md (still has unresolved arch items), then STATE.md, then MEMORY.md. 

**Likely first prompts in order:**
1. **"Answer the mcap-gate question"** — user picks option 1/2/3 for unknown-CIK handling. Then either draft PLAN or just implement (depends on user's framing).
2. **"Ingest May 13"** — once US trading day complete and SEC master.idx published. Use `python run_week.py --start 2026-05-13 --days 1`.
3. **"Commit accumulated work"** — 12+ untracked files across last 3 sessions.

**Don't presume.** Ask which to pick up.

## Loop position (unchanged)

Phase 18 of v1.7 still APPLY ✓ / UNIFY ○. Today's work was a) daily ingest catchup (operational), b) verification (read-only), c) design discussion for token-saving mcap gate (Phase 21 candidate, not approved or scoped). Loop did not advance.

---

*Handoff created: 2026-05-13 v2 — third pause in 48h (all power-shutdown-driven). Ingest pipeline behavior is healthy; user is back in driver's seat on signal-quality + cost-optimization design decisions.*
