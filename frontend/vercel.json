{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://RAILWAY_BACKEND_URL/api/:path*" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
