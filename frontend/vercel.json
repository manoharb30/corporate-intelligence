{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://api.lookinsight.ai/api/:path*" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
